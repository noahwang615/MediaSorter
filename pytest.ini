[pytest]
testpaths = tests
addopts = -vv -ra --tb=short -m "not docker"
log_cli = true
log_cli_level = INFO
markers =
    docker: tests that require a local Docker daemon and container build support
    mediasort: tests covering scripts/mediasorter.py sorting logic
    makeproofs: tests covering scripts/make_proofs.py proof creation logic